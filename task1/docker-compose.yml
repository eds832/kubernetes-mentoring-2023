version: "3.8"

services:
  users-db:
    image: postgres:15.2
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=1234
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
       
  posts-db:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=1234
      - MYSQL_DATABASE=posts-db
    ports:
      - "3307:3306"
    volumes:
      - mydb:/var/lib/mysql

  user-app:
    image: eds832/userapplication:1.0.0
    restart: on-failure
    ports:
      - "8081:8080"
    environment:
      SPRING_APPLICATION_JSON: '{
        "spring.datasource.url"  : "jdbc:postgresql://users-db:5432/users",
        "spring.datasource.username" : "admin",
        "spring.datasource.password" : "1234",
        "spring.jpa.properties.hibernate.dialect" : "org.hibernate.dialect.PostgreSQLDialect",
        "spring.jpa.hibernate.ddl-auto" : "create",
        "server.port" : "8080"
      }'
    depends_on:
      - users-db
    
  post-app:
    depends_on:
      - posts-db
    image: eds832/postapplication:1.0.0
    restart: on-failure
    ports:
      - "8082:8080"
    environment:
      SPRING_APPLICATION_JSON: '{
        "spring.datasource.url"  : "jdbc:mysql://posts-db:3306/posts-db?useSSL=false",
        "spring.datasource.username" : "root",
        "spring.datasource.password" : "1234",
        "spring.jpa.properties.hibernate.dialect" : "org.hibernate.dialect.MySQL5InnoDBDialect",
        "spring.jpa.hibernate.ddl-auto" : "create",
        "server.port" : "8080",
        "user.app.url" : "http://user-app:8080/users/"
      }'
    stdin_open: true
    tty: true

volumes:
  pgdata:
  mydb: